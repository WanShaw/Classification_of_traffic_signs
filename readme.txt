161_45_acc0.9868：模型参数文件；
delete.py：删除训练集中的部分错误标签文件；
labeltrans.py：删除json文件中的错误标签文件；
of_dataset1.py：数据集构造文件；
of_DenseNet.py：训练的模型文件；
of_predict.py：生成单个模型预测结果的文件；
of_predict1.py：生成多个模型预测结果的文件；
of_train1.py：训练文件，不利用标签平滑；
random_split.py：验证集和训练集分割文件；
submit_example.json：测试集的json文件；
train.json：训练集的json文件（分割前，删除错误标签图片前）；
train_list1.json：分割后的训练集json文件（删除错误标签图片后）；
train1.json：训练集的json文件（分割前，删除错误标签图片后）；
val_list1.json：分割后的验证集json文件（删除错误标签后）
版本说明
oneflow=0.8.0+cu112
flowvision=0.2.0